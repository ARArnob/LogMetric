# Copy this to .env.local for local development
# Set NEXT_PUBLIC_API_URL in Vercel project settings for production

# Your backend API URL (defaults to localhost:8080 if not set)
NEXT_PUBLIC_API_URL=http://localhost:8080/api
