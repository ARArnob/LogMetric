# Supabase project settings -- Project Settings -> API in the Supabase dashboard.
# Copy the two values into logmetric-ui/.env.local (same var names).
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_ANON_KEY=
